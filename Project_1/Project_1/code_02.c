/*#include<stdio.h>
// 定义一个宏变量，一般大写
#define PI 3.14
float sum(float r)
{
	float z = 0.0f;
	z = PI * r * r;
	return z;
	return z;
}

int main(int argc,char * argv[])
{
	//定义变量
	float r = 0.0f;
	float s = 0.0f;

	// 从键盘获取半径
	printf("请输入圆的半径r:");
	scanf_s("%f", &r);
	
	// 计算面积
	//s = PI * r * r;
	s = sum(r);

	// 输出面积值，保留小数点后两位
	printf("圆的面积是:%.2f\n", s);
	 
	return 0;
}
*/